# Clone source
# Create database
# setup
- Create database
- update file .env
- run command
    composer install
    php artisan key:generate
    php artisan migrate
    php artisan db:seed
    npm install
    npm run watch
